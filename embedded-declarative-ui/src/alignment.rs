#[allow(dead_code)]
#[derive(Clone, Copy)]
pub enum VerticalAlignment {
    Top,
    Bottom,
    Center,
}

#[allow(dead_code)]
#[derive(Clone, Copy)]
pub enum HorizontalAlignment {
    Left,
    Right,
    Center,
}
